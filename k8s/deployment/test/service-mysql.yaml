apiVersion: v1
kind: Service
metadata:
  name: ap-mysql
  namespace: ap-test
  labels:
    app: ap-mysql
    environment: testing
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30307
  selector:
    app: ap-mysql